---
 - hosts: all

   tasks:
   - name: copy six_pinp.log
     fetch:
      src: /var/log/six_pinp.log
      dest: fetchedfiles/six_pinp_par0342."{{ lookup('pipe', 'date') }}".log
      flat: yes
      
   - name: copy six_mngr.log
     fetch:
      src: /var/log/six_mngr.log
      dest: fetchedfiles/six_mngr_par0342."{{ lookup('pipe', 'date') }}".log
      flat: yes 